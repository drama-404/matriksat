// Root layout - minimal wrapper
// All i18n, fonts, and styling handled in [locale]/layout.tsx

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
